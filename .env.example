# Required only when using the captured Shopify Tool Gateway MCP server.
TOOL_GATEWAY_TOKEN=replace-with-your-token

# Optional. Use an absolute path when OpenCode starts with a reduced PATH.
# DCG_BIN=/Users/you/.local/bin/dcg
